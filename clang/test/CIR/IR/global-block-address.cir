// RUN: cir-opt %s | cir-opt | FileCheck %s

!void = !cir.void

module {
  // CHECK: cir.global external @p = #cir.block_address<@f, "A"> : !cir.ptr<!void>
  cir.global external @p = #cir.block_address<@f, "A"> : !cir.ptr<!void>
}
